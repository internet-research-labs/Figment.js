function Mesh () {
  this.m.vertices = new Float32Array([]);
  this.m.indices  = new Uint8Array([]);
}

Mesh.prototype.addFace    = function (inFace) {
};

Mesh.prototype.addVertex  = function (inVertex) {
};

Mesh.prototype.addIndices = function (inIndices) {
};
